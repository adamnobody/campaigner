# 0.3.2 — Canvas architecture (scene types + scene_container)

**Статус:** **core implementation closed** на `testbed-rust` (2026-06-07)  
**ADR:** [`docs/adr/ADR-0004-canvas-scene-types-and-scene-container.md`](../adr/ADR-0004-canvas-scene-types-and-scene-container.md) — **Accepted**  
**Зависимости:** 0.3.1.x parity ✅ · Text parity 0.3.5 ✅ · PR9 legacy migrator **deferred**

---

## Два трека

| Трек | Версия | Миграция | Статус |
|------|--------|----------|--------|
| **Parity** | 0.3.1.x | 017 | ✅ закрыт |
| **Canvas architecture** | 0.3.2 | 018+ | ✅ core закрыт; PR9 deferred |
| **Text parity** | 0.3.5 | нет | ✅ закрыт (`d8b0f4664`) |

**Project Graph** — вне scope.

---

## PR-очередь (итог)

| PR | Название | Статус |
|----|----------|--------|
| **PR1** | Breadcrumbs + navigation stack | ✅ `navigationStack.ts`, marker drill-down |
| **PR2** | Territory parity dialog/panel | ✅ (0.3.1.x C2) |
| **PR3** | Multi-ring rendering | ✅ |
| **PR4** | ADR-0004 Accepted + docs | ✅ (2026-06-07 doc batch) |
| **PR5** | Migration 018+ | ✅ `018_canvas_scene_type.sql` |
| **PR6** | Backend `scene_type` + kind matrix | ✅ `validate_object_kind_for_scene`, root invariant |
| **PR7** | Atomic create map + scene_container | ✅ `canvas_create_map_scene_container` |
| **PR8** | Frontend scene_container MVP | ✅ reconciler card, toolbar tool, navigation `via: 'container'` |
| **PR9** | Legacy opt-in migration prompt | ⏸ **deferred** |

Дополнительно в ветке (вне исходного PR-списка): finite map viewport, scene background layer, creation preview crop.

---

## Deferred / future scope

Явно **не реализовано** и не входит в закрытие 0.3.2 core:

| Item | Примечание |
|------|------------|
| **PR9** legacy opt-in migrator | Detect root NULL + `background_path` → opt-in split to `root_canvas` + card + child `map`; no silent migration |
| Route rename `/map` → `/canvas` | route `/project/:id/map` сохранён |
| Unified `text` kind + `warp.mode` | `text` и `curve_text` остаются раздельными kinds |
| `entity_ref`, `connection`, `chart` | 0.4.x+ per vision |
| Old SQL dumps / offline `maps` → `canvas_*` migration | отдельная задача |
| CV04 layers UI, SD01/SD02 context panel | 0.3.2+ product track |
| Runtime performance debt | [`docs/canvas-debt.md`](../canvas-debt.md) |

---

## PR-очередь (детали, историческая спецификация)

Оценка: **S** ≈ 0.5–1 дн, **M** ≈ 1–3 дн, **L** ≈ 3–5 дн.

### PR1 — Parity breadcrumbs / navigation helper MVP — ✅

- `navigationStack.ts`, breadcrumbs в `MapToolbar`
- Vitest: `navigationStack.test.ts`
- `via: 'marker' | 'container' | 'root'`

### PR2 — Territory parity — ✅

См. `docs/plans/0.3.1.x-canvas-parity.md` (C2).

### PR3 — Multi-ring rendering — ✅

`geometryRingsFromJson`, `drawRingList` в `canvasReconciler.ts`.

### PR4 — ADR-0004 + docs — ✅

ADR status **Accepted**; vision, gaps, debt синхронизированы.

### PR5 — Migration 018+ — ✅

- `canvas_scene.scene_type` (nullable: `root_canvas` | `map`)
- `canvas_object.kind` + `scene_container`
- Partial unique index: один `root_canvas` per project
- **No backfill** NULL → types at migration time

### PR6 — Backend scene_type support — ✅

- `get_root_scene` / `create_root_scene_for_project` with `root_canvas`
- `validate_object_kind_for_scene` + matrix
- Ancestor check for `linked_scene_id`
- Regression: `attach_child_scene_to_marker`

### PR7 — Atomic create map + scene_container — ✅

- `canvas_create_map_scene_container` — TX map scene + layers + container
- Tests: success, rollback, invalid parent

### PR8 — Frontend scene_container MVP — ✅

- Reconciler card + linked scene background preview
- Double-click / Open → navigate + stack push
- Tool on `root_canvas` only; hidden on `map`
- Map creation flow with background upload

### PR9 — Legacy opt-in migration prompt — ⏸ deferred

Detect legacy root-with-background; opt-in dialog; **no silent migration**.  
Отложено до появления реальных legacy datasets у тестеров.

---

## Источники истины

| Документ | Роль |
|----------|------|
| ADR-0004 | scene_type, scene_container, invariants |
| `docs/prod/vision/canvas.md` | Продуктовая этапность |
| `docs/plans/0.3.1.x-canvas-parity.md` | Parity (closed) |
| `docs/canvas-functional-gaps.md` | Актуальные UX gaps |
| `docs/canvas-debt.md` | Runtime debt |

---

## Тесты (сводка)

| Область | Статус |
|---------|--------|
| Rust `scene_type` / `scene_container` / atomic TX | ✅ `cargo test` in `repositories/canvas.rs` |
| Frontend navigation | ✅ `navigationStack.test.ts` |
| PR9 migrator | — not implemented |
| E2E / Pixi snapshots | not required for MVP |

---

*Последнее обновление: 2026-06-07 — documentation sync with `testbed-rust`.*
