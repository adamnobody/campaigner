# Optional manual 16k background (Task 2)

Place a **16 384 × 16 384** PNG here as `background-16k.png` to test a real file
instead of the default procedural tiles. This path is gitignored (`*.png`).

The demo uses **procedural tiled sprites** by default; a future loader for this
file is not wired yet — procedural tiles are the validation path in REPORT.md.
